fn sub(a: usize, b: usize) usize: a - b

struct Foo:
    field1: usize^
    field2: u8^^

fn main():
    let x = 0
    sub(
        b:
            x = x + 1
        a:
            x = x + 1
            x
    )
