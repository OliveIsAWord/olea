fn sub(c: u8, a: usize, b: usize) usize: a - b

struct Foo:
    field1: usize^
    field2: u8^^

fn main():
    let x = 0
    sub(
        a:
            x = x + 1
            x
        c: x as u8
        b:
            x = x + 1
            x
    )
